# 数据传递微服务迭代协作事项

> 适用范围：`Product/DataTransfer`
>
> 用途：记录开发当前迭代时，为了保持迭代边界清晰而转入未来迭代的事项。这里不记录普通想法或长期愿望，只记录会影响后续实现、验收、跨服务对齐的待办。

## 维护规则

1. 每个事项都必须写明来源迭代、目标迭代、前置关联与完成标准。
2. 事项进入本文档后，必须在目标迭代内完成；若依赖其他微服务先完成接口或业务决策，则状态标为“外部前置”，并保留对应的 CSC 编号或对接对象。
3. 最终迭代收尾时，应逐条检查本文档。除仍被外部微服务阻塞的事项外，不应留下未完成项。
4. 若未来实现发现某事项不再需要，应把状态改为“已取消”，并写明取消依据；不要直接删除。
5. 本文档只记录 DataTransfer 自身视角的开发协作事项。跨服务总体约束仍以仓库根目录的 `跨服务协作备忘.md` 为准。

## 状态说明

| 状态 | 含义 |
|------|------|
| 待办 | 已明确需要在未来迭代实现，尚未开始 |
| 进行中 | 当前迭代正在实现 |
| 已完成 | 已实现、已测试，并且完成标准全部满足 |
| 外部前置 | 需要其他微服务先完成接口、语义或业务决策 |
| 已取消 | 经确认后不再需要实现，需保留取消原因 |

## 当前事项

| ID | 来源迭代 | 目标迭代 | 状态 | 类型 | 前置关联 | 待办事项 | 完成标准 | 来源 |
|----|----------|----------|------|------|----------|----------|----------|------|
| DT-COL-001 | P0 | P1 | 待办 | 本服务实现 | 无 | 实现 Modbus TCP Connector 与首个 Converter 闭环，接入 P0 Runtime 消息总线。 | 可完成寄存器读取、状态检测、写寄存器或线圈；上行生成 `DeviceMessage`；下行返回 `CommandResponsePayload`；具备单元测试和模拟设备测试。 | 软件设计 12.2 |
| DT-COL-002 | P0 | P1 | 待办 | 本服务实现 | 无 | 将 P0 的简化命令处理扩展为 Command Router 与 Ack Tracker。 | 按 `DeviceMessage.device.device_id` 路由到 Connector；支持超时、重试、能力不足返回、同步回执、异步回执；`command_id` 去重继续满足 `DT-CMD-005`。 | 软件设计 4.9、12.2 |
| DT-COL-003 | P0 | P1 / P3 | 外部前置 | 跨服务对接 | CSC-001，设备注册微服务 | 定义内部 `DiscoveryEvent`，P1 先记录日志与指标；P3 再接通设备注册微服务的发现受理接口。 | P1：发现事件可观测但不纳入正式采集。P3：设备注册微服务接口确认后，方案A/gRPC 与方案B/MQTT discovery 通道均完成对接。 | 跨服务协作备忘 CSC-001、软件设计 DQ-001 |
| DT-COL-004 | P0 | P2 | 待办 | 本服务实现 | 无 | 实现方案B可靠传输：Buffer Store、Replay Worker、断连恢复、续传限速和批量确认。 | SQLite pending/completed 两阶段确认可用；断连期间上行消息进入本地缓冲；重连后按时间次序补发；TTL 清理和容量限制具备测试。 | 软件设计 4.10、12.3 |
| DT-COL-005 | P0 | P2 | 待办 | 协议完善 | 可用 MQTT Broker 测试环境 | 补齐 MQTT 实际 Broker 场景验证，复查 QoS 2 指令链路成本，并评估是否需要升级到 MQTT v5 User Properties。 | 具备真实 Broker smoke test；command/config/cmd-response QoS 2 行为有测试记录；若保留 MQTT 3.1.1 兼容路径，需要记录原因与限制。 | 软件设计 DQ-006、接口文档 3.2 |
| DT-COL-006 | P0 | P3 | 外部前置 | 跨服务对接 | CSC-002，设备注册微服务 | 实现 Config Manager，处理 `DeviceConfigUpdate` 校验、幂等、差异分析与 `entity_revision` 乱序保护。 | `update_id` 重复推送幂等；`entity_revision` 小于或等于本地版本时忽略；大于本地版本时应用变更；设备注册微服务确认版本语义后完成联调。 | 跨服务协作备忘 CSC-002、软件设计 6.3、12.4 |
| DT-COL-007 | P0 | P3 | 待办 | 协议扩展 | MQTT Device / OPC-UA 测试环境 | 扩展 MQTT Device Connector 与 OPC-UA Connector。 | MQTT Device 支持设备侧 JSON telemetry/status/event/cmd-response；OPC-UA 支持 Subscription、Read、Write、Call 的首版能力；具备模拟服务测试。 | 软件设计 4.3、12.4 |
| DT-COL-008 | P0 | P3 | 待办 | 插件机制 | 无 | 完成插件隔离验证：进程内注册保留，sidecar 接口 proto 与 mock 实现补齐。 | 内置协议仍可进程内注册；sidecar mock 可通过本地 gRPC 或 Unix domain socket 交换上行消息、下行指令与状态查询。 | 软件设计 DQ-009、12.4 |
| DT-COL-009 | P0 | P3 | 外部前置 | 跨服务对接 | CSC-003，命令编排引擎 | 确认是否需要批量指令；如需要，新增 `SendCommandBatch(DeviceMessageBatch)` 或等价扩展。 | 命令编排引擎给出单条或批量需求结论；若需要批量，DataTransfer 完成接口、路由、去重与回执语义；若不需要，保留单条接口并记录确认结果。 | 跨服务协作备忘 CSC-003、软件设计 DQ-003 |
| DT-COL-010 | P0 | P3 | 外部前置 | 跨服务对接 | CSC-004，数据处理与聚合微服务 | 确认 `DeviceIdentity.tags` 的标签键和精确匹配过滤是否满足消费侧需求。 | 数据处理与聚合微服务确认标签键；DataTransfer 保持 `tag_match` 多条件交集过滤；复杂过滤若仍需要，由消费侧实现并记录边界。 | 跨服务协作备忘 CSC-004、软件设计 DQ-005 |
| DT-COL-011 | P0 | P3 | 待办 | 架构预留 | 信创与国密合规清单 | 补齐 `StorageBackend` 与 `TLSProvider` 抽象接口，为后续国产数据库和国密算法适配保留工程入口。 | 方案B缓冲不直接绑定 SQLite 细节；TLS 初始化不直接绑定单一实现；合规清单明确后可新增适配器。 | 软件设计 DQ-008 |
| DT-COL-012 | P0 | P1 | 待办 | 测试完善 | 无 | 给 P0 启动编排增加轻量启动测试或 smoke script，覆盖管理端和 gRPC 端口可启动。 | 可在随机端口启动服务并正常关闭；`/healthz`、`/readyz`、gRPC 服务注册状态均有验证。 | P0 实现回顾 |

## 本轮 P0 已知边界

P0 已完成 Go + Protobuf + gRPC/MQTT 的运行骨架，包含配置加载、管理端点、内存消息流、近期消息拉取、基础指标、`command_id` 去重和 P0 指令拒绝回执。

P0 尚未接入真实南向设备协议，因此所有依赖设备采集、协议转换、设备侧写入、配置热加载、断连续传的事项均登记在上表，并按 P1、P2、P3 分阶段完成。
