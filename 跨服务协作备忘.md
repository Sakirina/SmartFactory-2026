# 跨服务协作备忘

> **用途：** 记录各微服务需求讨论中产生的跨服务依赖、待对齐接口、遗留问题。每个微服务在独立会话中讨论，本文件是会话之间传递"包袱"的唯一通道。  
> **使用方式：**  
> 1. 当某个微服务的需求讨论中发现涉及其他微服务的问题，在此文件中登记一条条目  
> 2. 讨论另一个微服务时，先检查本文件中与该微服务相关的待办项  
> 3. 问题解决后更新状态为 `已解决`，注明解决方式和所在文档位置  
> **格式约定：** 每条条目包含来源、目标、状态、描述、解决建议  
> **最后更新：** 2026-06-19（新增 CSC-007、CSC-008、CSC-009：直接控制回执、云↔边可靠投递与去重、下行投递状态与背压（需编排控制中心配合）的云边协作，均仅概要登记、实现细节待相关微服务迭代；来源《智慧工厂云端平台需求基线》v1.0、《平台数据网关需求基线》v1.0。前次 2026-06-10：基线校准回写完成，需求/接口 v2.3、软件设计 v1.1 已生效，CSC-005 已解决，CSC-002/003 涉及内容已写入基线 v2.3）

---

## 待办条目

### CSC-001 设备注册微服务需定义「接收设备发现通知」的接口

| 字段 | 内容 |
|------|------|
| **来源** | 数据传递微服务需求讨论 |
| **目标微服务** | 设备注册微服务 |
| **状态** | `待讨论` |
| **描述** | 数据传递微服务支持自动发现设备（FR-S-005），发现后须上报至设备注册微服务等待确认（FR-S-006）。数据传递微服务侧的上报通道已定义（方案A: 调用设备注册微服务的 gRPC 接口；方案B: 发布至 `dt/v1/up/{gatewayId}/discovery`），但设备注册微服务侧的接收接口尚未定义。 |
| **需要设备注册微服务定义的内容** | 1. 接收发现通知的 gRPC Method（方案A） 2. 订阅 MQTT discovery Topic 后的处理流程（方案B） 3. 确认/拒绝设备的决策逻辑（自动规则 or 人工审批） 4. 确认后通过 `PushDeviceConfig` 或 `dt/v1/down/{gatewayId}/config` 下发正式配置 |
| **数据传递侧设计决策** | 软件设计 DQ-001：数据传递侧先定义内部 `DiscoveryEvent` 结构，P1 阶段用日志+指标记录发现事件，不阻塞开发。接口闭环等设备注册微服务确认后接通（预计 P3） |
| **相关文档** | `数据传递微服务需求分析文档.md` FR-S-005, FR-S-006; `数据传递微服务接口文档.md` 3.1.1, 3.2.2; `数据传递微服务软件设计.md` DQ-001 |

---

### CSC-002 设备注册微服务需定义「设备配置推送」的触发机制与数据格式对齐

| 字段 | 内容 |
|------|------|
| **来源** | 数据传递微服务需求讨论 |
| **目标微服务** | 设备注册微服务 |
| **状态** | `待讨论` |
| **描述** | 数据传递微服务已定义了接收配置推送的接口（方案A: `PushDeviceConfig` gRPC Method; 方案B: `dt/v1/down/{gatewayId}/config` MQTT Topic），包含 `DeviceConfigUpdate` 消息结构（支持设备/Connector 的增删改）。设备注册微服务作为调用方，需确认此接口是否满足其推送需求，或提出调整要求。 |
| **需要设备注册微服务确认的内容** | 1. `DeviceConfigUpdate` 的 `UpdateAction` 枚举是否覆盖所有操作场景（v2.0 新增 `UPDATE_GLOBAL`） 2. `DeviceConfigPayload` 和 `ConnectorConfigPayload` 的字段是否充分（v2.0 新增上报策略配置字段） 3. 批量推送需求（如一次性推送整个 Connector 下所有设备）是否需要额外接口 |

> **⚠️ 跨服务硬需求：`entity_revision`**
>
> `DeviceConfigUpdate` v2.2 新增字段 `entity_revision: int64`（字段编号 5）。**设备注册微服务必须为每个实体（device / connector）维护独立的单调递增版本号**，每次配置变更 +1，填入此字段后再推送。
>
> **为什么：** 数据传递微服务依据 `entity_revision` 判断推送是否过时。仅靠内部到达顺序编号无法区分"新配置"和"旧配置因网络延迟晚到"。没有此字段，乱序保护形同虚设。
>
> **契约：**
> - `entity_revision` 大于本地已应用版本 → 应用变更
> - `entity_revision` 等于本地已应用版本 → `update_id` 去重，忽略
> - `entity_revision` 小于本地已应用版本 → 过时变更，静默忽略
> - 以上三种情况均返回 `success=true`（幂等语义），设备注册微服务无需对 success 做重推判断
>
> **设备注册微服务需求侧纳入点：** 此条应作为设备注册微服务的功能需求之一——"配置推送须携带实体级版本号"。

> **⚠️ 跨服务硬需求 2：字节字段内嵌 JSON 的字段命名契约（P3.5 钉死；已回写接口文档 v2.3 §3.1.2，含 JSON 示例与 strategy_overrides / UPDATE_GLOBAL 语义）**
>
> `DeviceConfigPayload.address / datapoints` 与 `ConnectorConfigPayload.connection / polling / converter` 均为 `bytes`（JSON 编码）。数据传递微服务 P3.5 起将其 JSON 字段名钉死为 **snake_case，与本服务 YAML 配置字段一一对应**（如 `register_type`、`data_type`、`interval_millis`、`unit_id`、`cmd_response_topic`、`report_strategy.period_seconds`）。设备注册微服务生成这些 JSON 时必须使用相同命名；Go 默认字段名（`RegisterType` 等大写驼峰）不再被接受。完整字段清单见 DataTransfer `internal/config/config.go` 的 json 标签。
>
> **补充语义（P3.5 实现落地）：**
> - `DeviceConfigPayload.strategy_overrides`（字段 8）已接通：按 `key` 匹配设备数据点并写入数据点级上报策略；key 不匹配任何数据点时该条 override 被忽略并记 `DT-CFG-003` 警告。
> - `UPDATE_GLOBAL`：`default_report_strategy` 与 `backpressure_policy` 支持热更新即时生效；`queue_capacity` 为启动期参数，运行时推送非零值会被忽略并记 `DT-CFG-003` 警告（如需调整请改本地配置 `runtime.ring_size` 后重启）。
> - `BP_DEGRADE` 背压策略当前自动回退 `BP_BLOCK`（Connector 动态降频能力排期 P4，DT-COL-015）。

| **相关文档** | `数据传递微服务需求分析文档.md` v2.2 FR-S-029a; `数据传递微服务接口文档.md` v2.2 3.1.2 (`DeviceConfigUpdate.entity_revision`), 乱序保护说明; `数据传递微服务软件设计.md` 6.3 |

---

### CSC-003 命令编排引擎需对齐下行指令的 Protobuf 消息格式

| 字段 | 内容 |
|------|------|
| **来源** | 数据传递微服务需求讨论 |
| **目标微服务** | 智能命令编排与下发引擎微服务 |
| **状态** | `待讨论` |
| **描述** | 数据传递微服务已定义了下行指令的统一格式：`DeviceMessage` 信封（v2.2 新增 `command_id` 字段）+ `ControlPayload` / `ParamUpdatePayload` / `QueryPayload` 三种 Payload。命令编排引擎作为指令的发起方，需确认此格式能否满足其编排输出的表达需求。 |
| **需要命令编排引擎确认的内容** | 1. `ControlPayload.action` 的字符串标识方式是否足够灵活 2. `CommandOptions`（timeout, retry, priority）是否覆盖了编排引擎需要控制的全部参数 3. 异步指令模式下，回执通过 `CMD_RESPONSE` 上行消息返回，编排引擎是否需要额外的回调机制 4. 指令是否需要支持批量下发（当前接口为单条） 5. P3.5 落地的重试/默认超时语义（见下行“设计决策”第 5 条）是否符合编排引擎预期，尤其 CONTROL 默认 10s 是否过长/过短 |
| **数据传递侧设计决策** | **已写入基线 v2.2：** (1) `command_id` 已提升至 DeviceMessage 信封层（字段编号 6），与 `message_id` 职责分离——`message_id` 追踪消息传输，`command_id` 追踪指令业务生命周期。**命令编排引擎须为每条下行指令分配全局唯一 `command_id` 并填入信封**；(2) FR-S-012 + DT-CMD-005：重复 command_id 被拒绝；(3) 首版单条指令，接口预留批量扩展；(4) `action` 字符串作为索引键，实际协议映射由设备配置中的 action mapping 定义；(5) **P3.5 已实现重试与默认超时**——`CommandOptions.retry_count` 为权威重试次数（0=不重试），仅对传输/执行错误重试（REJECTED 等确定性结果不重试），间隔指数退避（200ms 起、上限 5s）；`CommandOptions.timeout_ms=0` 时按类型取默认超时：CONTROL 10s、PARAM_UPDATE/QUERY 30s。编排引擎若有不同的超时/重试预期，请在对齐时提出。该语义已回写接口文档 v2.3（CommandOptions 注释与 §3.1.3），需求 v2.3 同步修订“默认 3 次”为“调用方指定、0=不重试”（BC-D1 方案一，PM 已裁决） |
| **相关文档** | `数据传递微服务需求分析文档.md` v2.2 FR-S-012; `数据传递微服务接口文档.md` v2.2 2.2 (`DeviceMessage.command_id`), 3.1.1, 5.2 (DT-CMD-005); `数据传递微服务软件设计.md` 4.9 |

---

### CSC-004 数据处理与聚合微服务需确认 tags 过滤与聚合需求

| 字段 | 内容 |
|------|------|
| **来源** | 数据传递微服务需求讨论（团队反馈触发） |
| **目标微服务** | 数据处理与聚合微服务 |
| **状态** | `待讨论` |
| **描述** | 数据传递微服务已在 `DeviceIdentity` 中新增 `tags` 字段（如 workshop, line, zone, system），每条消息都携带设备归属标签。数据处理与聚合微服务作为主要消费者，需确认推荐的标签键是否满足其分组聚合需求。 |
| **需要数据处理微服务确认的内容** | 1. 当前推荐的标签键（workshop, line, zone, system, floor, building）是否满足聚合维度需求 2. 是否需要在 `SubscribeRequest.tag_match` 之外增加更复杂的过滤能力（如正则匹配、NOT 条件） 3. 标签变更时（如设备从产线3移到产线5），是否需要变更通知，还是依赖下一条消息自动携带新标签即可 |
| **数据传递侧设计决策** | 软件设计 DQ-005：首版只做精确匹配 `tag_match`（多条件取交集）。复杂过滤（NOT、OR、正则、层级匹配）是数据处理服务自身的职责，不由数据传递微服务承担。标签变更通过后续消息自动携带新标签，首版不发独立变更事件 |
| **相关文档** | `数据传递微服务需求分析文档.md` 5.4 (tags 约定), FR-S-008a/008b; `数据传递微服务接口文档.md` 2.2 (DeviceIdentity.tags), 3.1.2 (SubscribeRequest.tag_match); `数据传递微服务软件设计.md` 7.1, DQ-005 |

---

### CSC-005 接口文档 5.2 错误码表需回写扩展码并由各消费方知悉

| 字段 | 内容 |
|------|------|
| **来源** | 数据传递微服务 P3.5 收尾审计 |
| **目标微服务** | 基线文档维护（PM）；知悉方：智能命令编排与下发引擎、数据处理与聚合、设备注册微服务 |
| **状态** | `已解决`（2026-06-10：扩展码 DT-CMD-006/DT-CON-006/DT-NBI-003 已回写接口文档 v2.3 §5.2，并补“错误码仅作诊断参考”使用说明；经 PM 审批，基线校准 BC-B1） |
| **描述** | P3.5 将实现中的错误码与接口文档 5.2 表逐一对齐（此前实现私自复用了 DT-CMD-001~004 且语义与文档冲突，已纠正）。对齐后 DT-CMD-001=设备不在线/无路由、DT-CMD-002=执行超时、DT-CMD-003=转换失败/能力不足、DT-CMD-004=重试中、DT-CMD-005=重复指令,与文档一致。同时实现需要文档未覆盖的错误场景，新增扩展码：**DT-CMD-006**（指令格式/参数无效，REJECTED/InvalidArgument）、**DT-CON-006**（Connector 配置/协议无效）、**DT-NBI-003**（北向消息 Protobuf 解码失败）。另确认 DT-CFG-001 按文档语义用于"热加载成功" INFO 日志，DT-CFG-002 用于"热加载失败"。 |
| **需要对齐的内容** | 1. 接口文档 5.2 表补录 DT-CMD-006、DT-CON-006、DT-NBI-003 三个扩展码 2. 消费方若解析 `CommandResponsePayload.message` 前缀的错误码，按对齐后语义处理（错误码仅作诊断参考，业务判断请依赖 `status` 枚举） |
| **相关文档** | `数据传递微服务接口文档.md` 5.2；DataTransfer `internal/errors/codes.go`；`docs/iteration-p3.5-review.md` |

---

### CSC-006 MQTT v5 User Properties 透传能力确认（DT-COL-013 外部前置）

| 字段 | 内容 |
|------|------|
| **来源** | 数据传递微服务 P2 spike + P4 规划（`DataTransfer/docs/mqtt-v5-spike.md`） |
| **目标微服务** | 数据处理与聚合微服务、智能命令编排与下发引擎微服务（MQTT 消费端能力）；运维/部署（Broker 选型） |
| **状态** | `待确认` |
| **描述** | DataTransfer 生产 MQTT 路径默认 v3.1.1。是否提供可选 v5 adapter（User Properties 承载追踪元数据）取决于三方确认；确认前 DataTransfer 不投入 v5 实现（已锁定为 DT-COL-013，目标迭代 P4）。 |
| **需要各方确认的内容** | 1. 部署 Broker 是否支持 MQTT v5 且透传 User Properties / Response Topic / Correlation Data 2. 上游消费者（数据处理、命令编排）客户端是否消费 v5 properties；若仍有 v3 客户端，业务语义必须继续只依赖 Protobuf payload 3. 运维是否接受 DataTransfer 配置中新增 `protocol_version`（默认仍 3.1.1） |
| **已验证事实（spike）** | v5 发布端与 v3 订阅端可在现有 topic/payload 合约下共存；业务语义（command_id/message_id/回执）不得依赖 v5 properties；Broker Message Expiry 不替代本地 SQLite Buffer TTL |
| **相关文档** | `DataTransfer/docs/mqtt-v5-spike.md`、`DataTransfer/docs/数据传递微服务遗留问题.md`、`DataTransfer/docs/协作事项.md` DT-COL-013 |

---

### CSC-007 云端「直接控制」请求/回执的云边协作（概要登记，细节待迭代）

| 字段 | 内容 |
|------|------|
| **来源** | 云端平台需求讨论（《智慧工厂云端平台需求基线》3.3.3、6.2） |
| **目标微服务** | 边缘命令编排引擎、边缘↔云传输模块（边缘侧云-边数据网关）；知悉方：云端编排与控制中心 |
| **状态** | `待讨论`（仅概要登记；具体请求/回执结构待相关微服务进入需求迭代时定义） |
| **描述（概要，不含实现）** | 云端编排与控制中心会对边缘发起「直接控制」——引用某个预定义预案或策略；该指令为即时控制（须工程师与上级领导在场、不存在预约），回执复用下发的同一 id、不另设请求标识。边缘依实时数据做情景研判：满足则执行并回报结果；不满足则回失败，并携带拒绝原因、风险等级、风险类别、是否需现场确认、上下文快照等结构化信息。云端据此按风险分级决定是否强制（流程/业务类走会签+审批，安全类需现场安全员现场确认）。这是一类**新的云边交互流量**，边缘侧云-边数据网关需预先知悉其存在与大致语义，以免日后协作撞墙。 |
| **需要边缘侧预先知悉/预留的内容** | 1. 云→边存在「直接控制」这一类下行流量（区别于规则/策略推送） 2. 边→云存在结构化「失败回执」回程（含风险评定与上下文快照） 3. 请求级标识用于请求与回执配对；指令级 command_id 仍由边缘命令编排引擎分配，二者不混用 |
| **明确不在本条范围（不涉及实现）** | 字段级 schema、Protobuf 定义、风险等级具体分档、传输与重试机制——均留待相关微服务需求迭代处理。本条只做协作面的概要登记。 |
| **相关文档** | `智慧工厂云端平台需求基线.md` 3.3.3、6.2 |

---

### CSC-008 云↔边可靠投递与去重的对称协作（概要登记，细节待迭代）

| 字段 | 内容 |
|------|------|
| **来源** | 平台数据网关需求访谈（《平台数据网关需求基线》§3.1、§5；Q4「谁接收谁负责」） |
| **目标微服务** | 边缘↔云传输模块（边缘侧云-边数据网关）、边缘数据传递微服务（已冻结，对齐参照）；云端平台数据网关 |
| **状态** | `待讨论`（仅概要登记；确认/重传/去重的字段与机制待相关微服务迭代时定义） |
| **描述（概要，不含实现）** | 云↔边每一跳遵循「谁接收谁负责」：接收方去重、发送方负责可靠投递（投到没有、要不要重投）。确认语义按数据性质两档——时序数据（传感/心跳）收到即确认、可容损、溢出丢最旧；非时序数据（事件/回执/配置/规则策略）持久存妥并去重才确认、丢不得、溢出持久保存等人工。去重以 message_id 为键，重传须复用同一 message_id/command_id。理念参照 TCP（确认、超时重传、接收端去重），并叠加端到端的应用级确认。 |
| **需要边缘侧对齐的内容** | 1. 云↔边两侧对称配备「发送缓冲 + 接收去重」 2. 两档确认语义与边缘既有的设备↔边缘可靠投递保持一致 3. 重传复用同一 id；接收方确认的含义按档区分 |
| **明确不在本条范围（不涉及实现）** | 字段级 schema、传输协议、确认/重传/去重的具体机制、去重窗口实现——均留待相关微服务需求迭代处理。 |
| **相关文档** | 《平台数据网关需求基线》§3.1、§5；边缘《数据传递微服务需求分析文档》v2.3 |

---

### CSC-009 下行投递状态与背压，需编排与控制中心配合（概要登记，细节待迭代）

| 字段 | 内容 |
|------|------|
| **来源** | 平台数据网关需求基线 §3.2.3（Q3 背压） |
| **目标微服务** | 编排与控制中心（下行发起方）；平台数据网关 |
| **状态** | `待讨论`（仅概要登记；队列实现、阈值默认值、信号协议待相关微服务迭代） |
| **描述（概要，不含实现）** | 网关下行收发以队列承载，设提醒 / 警告 / 错误三档水位（队列大小与阈值可配置）。网关向发起方如实回传投递状态（在途 / 已达 / 超时未达 / 失败），并在队列水位升高时按三档向编排与控制中心回传压力信号、施加背压，令其放缓乃至暂停下发。 |
| **需要编排与控制中心配合的内容** | 1. 消费下行投递状态（用于掌握指令是否送达、做超时处理） 2. 响应背压信号——按提醒/警告/错误三档放缓乃至暂停下发，避免把网关队列灌满 |
| **明确不在本条范围（不涉及实现）** | 队列实现、三档阈值默认值、压力信号的具体协议——留待相关微服务需求迭代处理。 |
| **相关文档** | 《平台数据网关需求基线》§3.2.3 |

---

## 已解决条目

（暂无）
